[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "backend"
version = "0.1.0"
description = "Backend package for SlideScribe (AI refinement and TTS)"
authors = [
    { name = "Your Name", email = "your@email.com" }
]
requires-python = ">=3.11"
